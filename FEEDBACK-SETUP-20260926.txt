Zynyro Feedback / Bug Report — FINAL SETUP

The frontend calls the Supabase Edge Function:
supabase/functions/submit-feedback/index.ts

Important fixes in this package:
1. supabase/config.toml is linked to project ref swrjhypfsbvlxlrpbsve.
2. submit-feedback is explicitly configured with verify_jwt=false; the function performs its own auth check with supabase.auth.getUser().
3. The frontend retries with a direct authenticated POST when supabase.functions.invoke() cannot reach the function, and shows the backend HTTP error instead of the generic request error.

SERVER-SIDE SECRETS (Supabase Dashboard / CLI only; NEVER put these in frontend .env or Git):
RESEND_API_KEY=<your Resend API key>
FEEDBACK_PRIMARY_EMAIL=shahulibrahim97@gmail.com
FEEDBACK_SECONDARY_EMAIL=lapshahultop123@gmail.com
FEEDBACK_FROM_EMAIL=<verified sender address in Resend>

Deploy from the project folder:
supabase.cmd link --project-ref swrjhypfsbvlxlrpbsve
supabase.cmd functions deploy submit-feedback --project-ref swrjhypfsbvlxlrpbsve

Set secrets before testing:
supabase.cmd secrets set RESEND_API_KEY=<your-key> FEEDBACK_PRIMARY_EMAIL=shahulibrahim97@gmail.com FEEDBACK_SECONDARY_EMAIL=lapshahultop123@gmail.com FEEDBACK_FROM_EMAIL=<verified-sender>

Then test:
1. Open Zynyro.
2. Open ⋮ -> Feedback / Bug Report.
3. Enter at least 5 characters.
4. Click SUBMIT REPORT.
5. Confirm the success message.
6. Confirm delivery to both configured recipient addresses.

If submission returns HTTP 503, the Edge Function is deployed but one or more server secrets are missing.
If submission returns HTTP 502, Resend rejected the email request; check the Resend sender/domain and API key.
If submission returns HTTP 401, the user's Zynyro session has expired or authentication failed.
